Title: Add GraphQL Codegen for types

Description:
Add GraphQL Code Generator to generate TypeScript types for API and clients to keep schemas and clients in sync.

Acceptance Criteria:
- `codegen.yml` or equivalent config present.
- `yarn run gen-types` (or script) generates types for server and client.

Labels: enhancement
