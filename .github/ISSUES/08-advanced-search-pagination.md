Title: Add advanced search, filters and pagination

Description:
Implement full-text search, advanced filter fields, sorting and paginated list endpoints for large datasets.

Acceptance Criteria:
- Search query parameter supporting multi-field search.
- Advanced filter endpoint for fields (city, type, language, etc.).
- Paginated results with `page` and `pageSize` parameters and `total` metadata.

Labels: enhancement
