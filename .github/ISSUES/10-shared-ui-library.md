Title: Create shared UI component library (React + Emotion)

Description:
Add a small UI component package (buttons, forms, lists) to be reused across frontends.

Acceptance Criteria:
- `packages/ui` or similar directory with published local package.
- Components documented and consumed by both frontends.

Labels: enhancement
